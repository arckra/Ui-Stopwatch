<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.snamer">

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Untuk reverse geocode (Geocoder) dan network check -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 13+ notifikasi -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SNAMER"
        android:theme="@style/Theme.Snamer">

        <!-- Splash sebagai LAUNCHER -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HOME / TIMER (MainActivity) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- STOPWATCH -->
        <activity
            android:name=".StopwatchActivity"
            android:exported="false" />

        <!-- BAG -->
        <activity
            android:name=".BagActivity"
            android:exported="false" />

        <!-- SHOP -->
        <activity
            android:name=".ShopActivity"
            android:exported="false" />

        <!-- PROFILE -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- LOGIN + REGISTER (dari Profile) -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileLoggedInActivity"
            android:exported="false"/>


    </application>
</manifest>
